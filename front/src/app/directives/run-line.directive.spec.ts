import {RunLineDirective} from './run-line.directive';

describe('RunLineDirective', () => {
  it('should create an instance', () => {
    const directive = new RunLineDirective();
    expect(directive).toBeTruthy();
  });
});
